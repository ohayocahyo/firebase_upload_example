name: firebase_upload_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # upload file to firebase
  firebase_core: ^1.0.3
  firebase_storage: ^8.0.3

  # pick file
  file_picker: ^3.0.1
  path_provider: ^2.0.11
  dio: ^4.0.6
  # gallery_saver: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true